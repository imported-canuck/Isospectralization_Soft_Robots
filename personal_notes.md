# Personal notes to keep track of my venvs/etc. Delete before pushing.

Tensorflow Implementation:
to enter venv (3.6.8): .\tf-cpu\Scripts\Activate.ps1
to exit venv: deactivate
to bypass block when entering venv: Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned

PT implementation:
to enter venv (3.11.9): .\pt-gpu\Scripts\Activate.ps1
to exit venv: deactivate
to bypass block when entering venv: Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned 

Make sure you're on correct working directory before entering venv
